<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Khatabook</title>
  <link href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css" rel="stylesheet" />
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: sans-serif;
  }

  html,
  body {
    width: 100%;
    height: 100%;
  }
</style>

<body class="relative overflow-hidden">
  <header class="flex items-center justify-between px-8 py-6 max-[820px]:px-4 max-[820px]:py-4 max-[400px]:p-3">
    <h1 class="text-xl">Khatabook</h1>

    <% var showLinks=typeof showLinks !=='undefined' ? showLinks : true %>
      <% if(showLinks) { %>
        <div class="flex items-center gap-5">
          <a class="home-button max-[400px]:text-xs" href="/profile">Home</a>
          <a class="create-new-hisab max-[400px]:text-xs" href="/profile/hisab">Create New Hisab</a>
          <a class="logout-button text-red-500 max-[400px]:text-xs" href="/logout">Logout</a>
        </div>
        <% } %>
  </header>